# Separation of concerns — prettier only owns JSON/web assets here.
#
# YAML is governed by yamllint (.yamllint.yaml) + actionlint.
*.yml
*.yaml

# Markdown is governed by markdownlint (.markdownlint.yaml) and the per-action
# README's are generated by action-docs — prettier must not fight either.
*.md

# Generated / vendored / volatile
node_modules/
.venv/
coverage/
out/
CHANGELOG.md
RELEASE_NOTES.md

# Tooling-internal session data and IDE files
.remember/
.idea/
.act/
